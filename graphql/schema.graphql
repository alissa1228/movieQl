# 사용자가 뭘 할 지에 대해서 정의하는 곳.

type movie{
    id:Int!
    title:String!
    rating:Float!
    summary:String!
    language:String!
    genres:[String]!
    medium_cover_image:String!
}

type Query{
    movies(limit: Int, rating:Float): [movie]!
}

